version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./hackerspace.db:/app/hackerspace.db
      - ./static/uploads:/app/static/uploads
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
