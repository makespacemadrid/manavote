version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_DEBUG: "false"
      FLASK_SECURE_COOKIES: "false"
    volumes:
      - ./app.db:/app/app.db
      - ./static/uploads:/app/static/uploads
    restart: unless-stopped
